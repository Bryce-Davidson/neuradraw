Layer annotations options
{
    {layer}:
        dotted: [true, false]
        {above}:
        {below}:
            dimensions: [x, y] 
            type: "latex", "text"
            text: "\\int{x^2}"
        {within}:
            type: "latex", "text"
            text: "\\int{x^2}"
    [nodes]:
        {above}
        {below}
        dimensions: [x, y] 
            type: "latex", "text"
            text: "\\int{x^2}"
        {within}:
            type: "latex", "text"
            text: "\\int{x^2}"
}